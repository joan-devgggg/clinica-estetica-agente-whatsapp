# Sesión nocturna 14/08/2026 — verificaciones y arreglos

Reglas de la noche: sin push, sin aplicar migraciones, sin saliente real, San Remo intacto.
Un commit por bloque; este fichero acumula el informe.

## Bloque 1 — Verificación de la propia auditoría (solo lectura)

**1a · `wa_message_id` en `messages`: 315 de 543 filas lo llevan — y el corte es limpio.**
Cero antes del 05/08 01:11 UTC; desde entonces, el 100 % (las 17 filas sin wamid
posteriores al día 4 son todas DEL día 4, antes del cambio de esa noche). El «0 de 219»
del 04/08 era cierto entonces y dejó de serlo esa misma noche. La explicación de H5
sobrevive: el UNIQUE está activo para todo el tráfico actual y las dos filas `[image]`
del 13/08 llevan wamid. **Matiz honesto**: la *redelivery* de Meta no es demostrable sin
los logs del proceso (Railway); lo demostrado es (a) 3 salientes con wamid propio frente
a 2 entrantes guardados, (b) la rama de media no pasa por NINGÚN dedupe (ni sesión ni
`buffer.seenKeys` — hace `return` antes del buffer, y en el primer mensaje de una
conversación ni siquiera existe sesión que consultar), y (c) `saveMessage` traga el
duplicado (`mensaje_duplicado_ignorado`) mientras la rama contesta sin mirar su
resultado. La hipótesis alternativa —una tercera foto real cuyo INSERT falló por otra
causa igualmente silenciada— produce la misma firma en BD; el dedupe del bloque 3 cubre
la primera y es inocuo para la segunda.

**1b · El recordatorio de Barbora salió por PLANTILLA, y los dos fallos son distintos.**
Su primer mensaje entrante DE LA HISTORIA es del 13/08 08:06 → a la hora del envío
(~08:00–08:06) `getLastInboundAt` devolvía null → `isWithin24hWindow(null)` = false →
`resolveAutomatedSend` = `template` (con `plantilla_recordatorio` es configurada; la
marca `contacts.recordatorio_enviado=true` lo confirma). `sendReminderMessage`
(reminder.js:271-328) **no llama a `saveMessage` en ninguno de los dos caminos** — ni
plantilla ni texto libre — así que el fallo «no está en messages» afecta a los dos, no
solo a la plantilla. Y son dos fallos separados:
- *No está en `messages`* → el Monitor enseña «Hola, si confirmado» como primer mensaje
  de la nada, y `last_message_at` no se mueve.
- *No está en `session.history`* → **este es el que produjo la mala respuesta**: el
  prompt del LLM se construye exclusivamente de `session.history` (verificado: bot.js no
  lee la tabla `messages` en ningún punto del flujo de conversación). Escribir la
  plantilla en `messages` arregla el panel, NO la ceguera del bot.

**1c · La línea es `bot.js:6315`, y el alcance es mayor que «responder al recordatorio».**
Al final de cada turno: `if (!session.leadGuardado && partialData.telefono &&
partialData.nombre) saveLead(orgId, { ...partialData, estado_cita: 'pendiente', ... })`
— el `'pendiente'` va EN DURO. La cadena, verificada eslabón a eslabón:
- sesión nueva → `bot.js:4043` hidrata `partialData.nombre` desde la ficha («Barbora
  Jalova», creada el 15/07);
- `leadGuardado` nace false (`bot.js:221`); los únicos sitios que lo ponen true
  (escalada, finalizar reserva, rehidratación SQLite) no corrieron en su turno;
- `db.js` `saveLead` → contacto existente → `updateLead` → `if (datos.estado_cita !==
  undefined) updates.estado = datos.estado_cita` — **sin ninguna condición sobre el
  estado previo**.
Evidencia de que corrió: `contacts.updated_at = 08:08:30.144`, dentro de la ventana del
turno 2 (entrante 08:08:20.681, saliente 08:08:30.355). Que ANTES estaba en
'confirmado' sigue siendo inferencia (contacts no guarda histórico), pero es la única
compatible con la marca: `recordatorio_enviado=true` solo la escribe
`marcarRecordatorioSent` sobre filas que `getLeadsPendientesRecordatorio` devolvió, y
esa consulta filtra `estado='confirmado'`; la ficha además apunta a ESTA cita
(`fecha_cita=2026-08-14`, `appointment_id=a7ada49d`). **Consecuencia general**: cualquier
clienta con estado 'confirmado' que escriba CUALQUIER COSA con sesión fresca y nombre en
ficha queda regresada a 'pendiente'. Un «gracias!» la víspera basta para perder el
recordatorio si aún no salió. Refuerza el diseño del bloque 2: el recordatorio no puede
colgar de un campo que la conversación pisa.

**1d · El cuarto test del Monitor no es vacío: mide pureza, no la clave.**
Saboteado en las dos direcciones (cp previo, regla 7): con el sort por `updated_at`
fallan los 3 primeros bloques y el 4º pasa; quitando el spread (`[...leads]` → `leads`,
sort in-place) falla SOLO el 4º. Protege que un refactor no convierta a `getAllLeads` en
mutador de la lista recibida. Le he añadido el comentario que lo deja dicho.

## Bloque 2 — H1 (el bot ve sus recordatorios) y H2 (el recordatorio cuelga de la cita)

**La hipótesis, evaluada primero: escribir las plantillas en `messages` arregla 2 de 3.**
Sí arregla el Monitor (el «Hola, si confirmado» deja de ser el primer mensaje de la nada)
y sí arregla `last_message_at` (con el orden nuevo del Monitor, el chat sube cuando sale
el recordatorio). **No arregla la ceguera del bot**: el prompt se construye SOLO de
`session.history` (1b). Así que el arreglo son DOS piezas, no una ni tres:

1. `registrarRecordatorioEnviado` (reminder.js), tras cada envío que Meta aceptó:
   `saveMessage` como `sender='bot'` — texto libre: el literal; plantilla: prefijado
   `[plantilla <nombre>]`, porque los bytes exactos los renderiza Meta y no se afirman
   (regla 4). Nunca lanza: el mensaje ya salió, y un fallo del registro no puede
   devolverlo a la cola (probado: se loguea y el marcado sigue).
   *Efecto lateral asumido y anotado en el código*: `saveMessage` refresca
   `conversations.last_message_at`, que auto-return usa como «última actividad» — un
   recordatorio retrasa hasta 24 h un retorno-a-auto de 7 días. La ventana de 24 h de
   Meta NO se toca (se calcula sobre entrantes).
2. `services/pending-outbound.js` (módulo nuevo, sin dependencias): el worker ANOTA el
   saliente y `processMessageCore` lo DRENA a `session.history` en el siguiente turno,
   ANTES del mensaje de la clienta (el orden real). Módulo propio para que reminder.js no
   requiera bot.js entero (los tests herméticos del worker no lo soportarían). Clave
   normalizada a dígitos (el worker conoce `34603…` y el bot `34603…@c.us`). TTL 48 h.

**H2 — el recordatorio del salón ahora cuelga de `appointments`.** El enrutado vive en
`db.getAppointmentsPendientesRecordatorio` (salón → citas; San Remo → ficha byte por
byte) — ahí y no en reminder.js A PROPÓSITO: los 5 tests herméticos del worker stubean db
entero y siguen funcionando sin enterarse. La consulta nueva: `status='confirmed'` +
`appointments.recordatorio_enviado=false` (columna que existía sin uso) + `starts_at`
futuro; el marcado va a la cita (`updated_by='worker:reminder'`, migración 033); mover
una cita por `updateAppointment` resetea el flag (volver a deber el recordatorio); y
`construirPendientesDesdeCitas` (pura, exportada) lleva la **guarda de transición**: si
la FICHA dice que el recordatorio de ese mismo día ya salió, la cita no entra — es lo que
hace indiferente el orden código/migración.

**Medido ANTES de elegir el arreglo (19 citas futuras confirmadas de Sante):**
12 se comportan igual · **2 GANAN** (Dasha Kotenko — ficha 'pendiente', recordatorio
imposible; Nieves Armengol — ficha con fecha vieja 08/08 y cita el 29/08, ventana
incalculable) · **0 reciben doble** (la guarda excluye a las 6 ya recordadas, Barbora
incluida) · 0 fantasmas hoy (todas las fichas viejo-elegibles tienen cita real detrás).

**Migración escrita y NO aplicada**: `supabase/migrations/041_recordatorio_por_cita.sql`
— backfill de la marca a appointments (6 filas medidas), con SELECT de comprobación en el
propio fichero. Idempotente; el código no la necesita para ser seguro (guarda), la
necesita para que appointments quede como fuente única.

**Red**: `tests/recordatorio-por-cita.test.js` (12 bloques: mapeo Madrid, guarda,
huérfanas sin silencio, enrutado por org con doble de supabase consultable, marcado) y
`tests/recordatorio-registro-conversacion.test.js` (4: worker real de punta a punta —
texto libre, plantilla, San Remo intacto, registro que falla sin reenviar). **Cuatro
sabotajes con cp previo, los cuatro en rojo**: sin guarda, sin enrutado, sin registro,
sin rama de marcado por cita. Suite entera + los 5 tests viejos del recordatorio: verde.

**Límites dichos:**
- El cableado en bot.js (las 3 líneas que drenan al historial) no tiene test de turno
  completo: no existe arnés hermético de turno con LLM stubeado y no era noche de
  construirlo. Cubierto por el test del módulo (incluida la normalización `@c.us`, que
  era el punto frágil) y el del worker; el seam queda leído, no probado.
- `bot.js:6315` (el `estado_cita:'pendiente'` en duro de fin de turno) **no se toca**:
  con H2 ya no cuesta recordatorios, pero sigue regresando el embudo del panel en cada
  turno. Cambiar su semántica afecta a San Remo y es decisión de diseño para el día.
- **Aviso operativo**: la cita de Dasha es HOY 14/08 a las 11:00; nada de esto está
  desplegado, así que su recordatorio no va a salir. Avisarla a mano si se quiere.

## Bloque 3 — H5: la media sin dedupe (2 fotos, 3 avisos)

`mediaMessageDedupe` (bot.js, wamid, TTL 60 s como el resto): la rama de media hace
`return` antes del buffer, así que ninguna de las redes existentes veía una redelivery
del webhook con el mismo wamid — el INSERT duplicado moría en el UNIQUE en silencio y el
aviso «No puedo ver fotos» salía otra vez. El guard va ANTES del `saveMessage` y del
aviso, con `has`+`add` síncronos y seguidos (cierra también la carrera de dos entregas
casi simultáneas). Dos fotos DISTINTAS siguen recibiendo su aviso cada una: el guard es
dedupe, no agrupación — agrupar avisos de una ráfaga sería un cambio de conducta aparte
y no se ha hecho.

De propina: los timers de limpieza de `TTLMessageDedupe.add` llevan ahora `.unref()`
(la regla de los timers de arranque aplicada a los de por-mensaje: un test que tocara el
dedupe se quedaba 60 s colgado esperándolos).

Red: `tests/media-dedupe.test.js` (3 bloques, conduce el `handleIncomingMessage` real
con db/telegram stubeados — la media no llama al LLM). Sabotaje con cp previo: sin el
guard, 2 de 3 en rojo. Suite entera en verde.

Matiz honesto que arrastra el bloque 1a: la redelivery no es demostrable sin los logs de
Railway; la firma en BD (3 salientes con wamid propio, 2 entrantes guardados) es
consistente con ella, y la alternativa —una 3ª foto real cuyo INSERT falló por otra causa
silenciada— también existe. El guard cubre la primera y es inocuo para la segunda; el
tercer bloque del test (la fila se guarda una vez) deja además de depender del UNIQUE
silencioso.

## Bloque 4 — El arnés verify:robustez:llm

**La pregunta de seguridad, primero, contra el código:**
- **Supabase de producción: SÍ escribe**, por diseño y con red doble (teléfonos `999…`
  — prefijo E.164 sin asignar, excluido de toda audiencia — + cleanup por escenario +
  barrido final por prefijo). Es lo que le da valor: catálogo y horarios de verdad.
- **WhatsApp real: NO puede.** El cliente de cada conversación es un sink; bot.js no usa
  `resolveOutboundClient` en ningún punto (verificado por grep — lo usan panel y
  workers); el arnés no arranca ningún worker ni registra clientes en el Map.
- **Telegram: era inerte POR ACCIDENTE** (`_botInstance` null sin `startTelegramBot` →
  `notifyOrgAdmin` avisa y devuelve false). Un accidente del orden de arranque no
  aguanta 100 escenarios × 6 workers: ahora el arnés **stubea `services/telegram` en
  require.cache** — imposible desde ese proceso, pase lo que pase aguas adentro.
- **`setBotActivo(ORG, true, false)`**: el tercer parámetro es `persist=false` — solo
  memoria del proceso; NO toca `config.bot_activo` de producción.

Veredicto: seguro para correr escenarios (sin saliente real posible); la paralelización
multiplica el churn de contactos de prueba en la Supabase real y los cleanups no pueden
compartir teléfonos → resuelto con el dígito de shard (abajo), pero **sin estrenar**.

**Lo hecho (en el orden pedido):**
- **(2) Reintento del degradado suelto**: `escenario()` reescrito sobre `intento()`
  aislado (teléfono y Convo propios). Un DEGRADADO a la primera se repite ANTES de
  contarse; si la 2ª va OK → «OK al repetir (la 1ª degradó: …)»; si no → «PERSISTE en 2
  corridas». **Solo DEGRADADO**: la fila dura (BUG/SILENCIO/BUCLE/ERROR) no se reintenta
  jamás — un fallo duro intermitente es hallazgo, no ruido.
- **(3) Corte por proveedor caído**: 3 escenarios seguidos contestando el fallback del
  modelo (los cuatro literales de `getFallbackResponse`, es/en/ru/uk) → corrida ABORTADA
  con exit 2 y diagnóstico impreso. Hasta hoy era una instrucción de lectura manual.
- **(4) Etiquetas familia/idioma**: los 25 escenarios etiquetados (A afirma-sin-respaldo
  · B red-ancha · C dato-sin-sitio · D idioma · E contexto-no-visto · control) y resumen
  agrupado al final. También son selectores: `-- familia:C`, `-- idioma:ru`, `-- 5-12`.
  La foto que dejan las etiquetas: 25 escenarios = 18 C · 4 A · 2 control · 1 D · 1 E,
  y 23 de 25 en castellano — el mapa de la tarde, ahora medible en el propio arnés.
- **(1) Paralelización: ESCRITA, SIN CORRER** (como pedía la orden): `-- shard:i/n`
  reparte por número de escenario (módulo n, probado sin huecos ni solapes) y el
  teléfono de prueba lleva el dígito del shard para que dos workers no se pisen los
  cleanups. El día del estreno: N procesos `node tests/verify-sante-robustez-llm.js
  shard:i/N` y sumar resúmenes. No se ha lanzado ninguna corrida en paralelo.

**La parte pura vive en `tests/lib/robustez-llm-helpers.js`** con test determinista en
`npm test` (`tests/robustez-llm-arnes.test.js`, 7 bloques; tres sabotajes en rojo:
un idioma quitado del detector, el corte sin reset, la fila dura reintentada). Corrida
de humo real del arnés (`-- 1`): OK, con resumen agrupado — un escenario, ~2 llamadas al
modelo, contactos TEST con cleanup, cero salientes.
